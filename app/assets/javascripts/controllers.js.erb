var galleryControllers = angular.module("galleryControllers", []);

galleryControllers.controller('GalleryCtrl', function($scope, $http, $window, Gallery){

  $scope.initializeData = function(){
    $scope.galleries = [];
    $scope.images = [];
  };

  $scope.initializeData();
});
