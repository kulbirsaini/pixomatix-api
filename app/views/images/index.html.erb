<div class="row">
  <div class="column-md-12">
    <p id="notice"><%= notice %></p>
  </div>
</div>

<div class="row">
  <% @images.each do |image| %>
    <div class="col-md-1">
      <%= link_to image_tag(image.images.first.try(:id) || 'default_gallery_image.png', class: 'img-responsive img-thumbnail'), image, class: 'thumbnail' %>
    </div>
  <% end %>
</div>
